
<div class="grayout">
  <%= link_to "", "/",class:"to_top" %>
  <div class="mordal">
    <%= link_to(image_tag("x_btn.svg"), "/", class:"x-btn") %>
    <div class="items">
      <%= form_tag("/users/#{@user.id}/update", {multipart: true}) do %>
        <div class="item">
          <p>ユーザーID <% if @error_message %><span>*</span><% end %></p>
          <input name="name" value="<%= @user.name %>" autofocus>
        </div>
        <div class="item">
          <p>新しいPassword <% if @error_message %><span>*</span><% end %></p>
          <input name="password" type="password">
        </div>
        <div class="item">
          <p>Password (確認) <% if @error_message %><span>*</span><% end %></p>
          <input name="password_confirmation" type="password">
        </div>


        <div class="item">

          <p class="errors">
            <% @user.errors.full_messages.reverse_each do |message| %>
              <span><%= message %></span><br>
            <% end %>
          </p>

        </div>

        <input type="submit" value="更新" class="btn sign-up">
      <% end %>
    </div>

  </div>

</div>
